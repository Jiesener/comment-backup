# 评论备份站

评论站的**只读**静态备份，托管在 GitHub Pages。

- 访问：`?u=<账号>`（`ycy` / `hxy` / `share` / `yanghao`）
- 数据：`data.json`，NAS 评论池导出快照，**不含任何凭据**
- 只读：不写入任何远端数据；"已取过"只记在本机 localStorage

主站（NAS）不受影响，此站仅为外网可用性兜底。
